<div class="contact-me-wrapper">
	<span class="line-break-top"></span>
	<h2>Contact Me</h2>
	<form id="contact-me">
		<label
			>Name
			<input type="text" name="name" required autocomplete="text" placeholder="Evan Trujillo" />
		</label>
		<label
			>Email Address
			<input
				type="email"
				name="email"
				required
				autocomplete="email"
				placeholder="evantrujillo30@gmail.com"
			/>
		</label>
		<label
			>Message
			<textarea name="message" required placeholder="How can I help?"></textarea>
		</label>
		<button>Send Message</button>
	</form>
</div>

<style>
	.contact-me-wrapper {
		display: grid;
		grid-template-areas:
			'h2 . .'
			'form form form';
	}

	.line-break-top {
		display: inline-block;
		grid-area: line;
		border: solid var(--light-grey-text-field) 1px;
	}

	h2 {
		grid-area: h2;
	}

	form {
		display: flex;
		grid-area: form;
		flex-direction: column;
		gap: 20px;
		margin-bottom: 50px;
	}

	label {
		display: flex;
		flex-direction: column;
		opacity: 70%;
		color: var(--grayish-dark-blue);
		font-weight: bold;
	}

	input,
	textarea,
	button {
		margin-top: 10px;
		outline-style: none;
		border-style: none;
		padding-left: 15px;
		font-size: 0.813rem;
		font-family: var(--public-sans);
	}

	input,
	textarea {
		outline: none;
		background-color: var(--light-grey-text-field);
		max-width: 100%;
	}

	input {
		height: 48px;
	}

	textarea {
		padding-top: 15px;
		height: 80px;
	}

	button {
		cursor: pointer;
		background-color: var(--dark-blue);
		padding: 17px 0px;
		max-width: 200px;
		color: var(--light-grey-text-field);
		letter-spacing: 0.1rem;
		text-transform: uppercase;
	}

	input:active,
	input:focus,
	textarea:active,
	textarea:focus {
		outline: 2px solid var(--primary-hover-color);
	}

	@media (min-width: 1024px) {
		.contact-me-wrapper {
			grid-template-areas:
				'line line line'
				'h2 form form';
			gap: 0px 62px;
		}
	}
</style>
